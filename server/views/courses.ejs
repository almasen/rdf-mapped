<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Courses - RDF Mapped</title>
    <meta property="og:type" content="">
    <meta name="description" content="Vitae Researcher Development Framework Mapped to Linkedin Learning">

    <%- include('./components/stylesheets'); -%>
    <%- include('./components/scripts'); -%>
</head>

<body>
    <%- include('./components/header'); -%>

    <main class="page faq-page">
        <section class="clean-block clean-faq dark">
            <div class="container">
                <%- include('./components/floating-back-button'); -%>
                <div class="block-heading" style="padding-top: 25px;">
                    <h2 class="text-info">Courses</h2>
                    <% if (pageData.data.length > 0) { %>
                        <p>Simply click on a row to open up a course's page.</p>
                    <% } else { %>
                        <p>We couldn't find any courses with these search parameters. <i class="material-icons icon">sentiment_dissatisfied</i><br>Please consider adjusting the filters.</p>
                    <% } %>
                </div>
                <div class="row">
                    <div class="col" style="max-width: 25%;"></div>
                    <div class="col" style="min-width: 438px;">
                        <%- include('./components/results-list'); -%>
                    </div>
                    <div class="col" style="max-width: 25%;"></div>
                </div>
            </div>
        </section>
    </main>

    <%- include('./components/footer'); -%>
</body>

</html>